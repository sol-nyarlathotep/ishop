package org.sol.shop.utils;

import org.apache.commons.codec.digest.DigestUtils;
import org.sol.shop.models.User;

import java.util.ArrayList;
import java.util.Arrays;

public class HashUtils {

    public static String hashUserToString(User user){
        // TODO: Implement
//        System.
//        byte[] bytes = {user.getId().byteValue(), user.getLogin().getBytes()[] };
        return "";
    }

    public static byte[] hashUser(User user){
        return new byte[]{};
    }

}
